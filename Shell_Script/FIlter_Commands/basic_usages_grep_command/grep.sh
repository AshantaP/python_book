grep "line1" "filename"     #This is find the word "line1" inside the "filename"
grep "bash" *   #This will try to find "bash" in current locations (no matter how many files are there)


cat text.txt | grep "above" #This command will try to find "above" in text.txt file


echo "==================I============================"
grep -i "Above" test.txt    #This option(-i) will ignore the case sensivity

